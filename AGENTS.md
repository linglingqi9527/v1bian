# AGENTS.md

本文件写给 Codex / AI 开发代理，用来约束后续对 `v1bian` 项目的修改方式。项目产品名为「辩了么」，当前阶段是：看比赛页和数据导入已有基础成果，接下来回到前端框架、写赛评和做训练流程收口。

## 先读顺序

新对话接手项目时，请优先阅读：

1. `AGENTS.md`：开发规则和边界。
2. `docs/CURRENT_STATUS.md`：当前真实进度。
3. `docs/NEXT_HANDOFF.md`：视觉、交互、下一步范围的短交接说明。
4. `README.md`：项目总览。

## 基本原则

- 不要推倒重写项目。先理解现有 `src` 分层、页面结构和 design-system，再做小步修改。
- 不要把正式产品做成长网页、后台管理系统或营销落地页。它应保持固定 app shell：外部背景、居中应用窗口、左侧导航栏、右侧主操作区。
- 大改前先说明计划，小步修改，每一步都要尽量保持项目可运行。
- 每次修改后必须保证 `npm.cmd run build` 通过；涉及 lint 风险的修改也应运行 `npm.cmd run lint`。
- 不要同时大改视觉、功能、数据、爬虫。每轮只处理一个明确目标。

## 当前阶段边界

- 看比赛页面已有较完整的数据导入、搜索、筛选、收藏、已看和 B 站跳转交互。
- 写赛评、做训练、我的页面仍有大量 demo，占位内容不能当作正式功能。
- 爬虫和语音识别数据管线已经做过一轮，当前暂时告一段落。
- 后续前端框架任务中，不要继续修改爬虫脚本、语音转写缓存或 `src/data/generated/generatedMatches.json`，除非用户明确要求进入数据阶段。
- 不要在 React 页面里实时请求 B 站；爬虫只作为开发期 Node 脚本运行，React 只读取生成后的 JSON。
- 当前不做后端、云登录、在线账号系统、产品内 AI 功能。

## 代码边界

- `roughjs` 只能在 `src/design-system/handdrawn` 内部 import。
- 不要在 `pages`、`features`、`styles` 中直接 import `roughjs`。
- 不要恢复 `RoughSelectionLayer` 作为主方案。它已废弃，不再依赖全局 DOM 扫描来生成选中态。
- 手绘选中填充由 `HandDrawnSelectionFill` / `HandDrawnAnimatedFill` 承担，具体使用以现有组件为准。
- 页面不要直接写 roughjs、SVG 抖动参数、active 填充动画。
- 全站按钮、标签、卡片优先使用 `SketchButton`、`SketchTag`、`SketchCard`。
- 如果发现页面里重复写卡片边框、按钮 active 填充、标签颜色，应优先收口到 design-system，而不是继续在页面里补样式。

## 视觉与素材

- 手绘风格组件必须集中管理，页面不能各自乱写一套手绘样式。
- 当前满意并应保留的方向：
  - 固定 app shell 和外围大边框。
  - 看比赛页面的小边框整体风格。
  - 小状态标签的 hachure 选中填充。
  - 收藏书签的手绘边框和黄色 hachure 填充。
- 当前仍可小步打磨的方向：
  - 大面积 hachure 填充真实度。
  - 右侧操作按钮的图标、文字对齐。
  - 比赛卡片右侧小竖杠。
  - 左侧导航整体比例和细节。
- 插图、图标、logo、纹理统一放在 `src/assets` 下对应目录。
- 用户数据、录音、录像、赛评正文、训练备注不要放进 `src/assets`。

## 产品规则

- 核心对象是比赛。赛评和训练都围绕比赛展开。
- 一个比赛对应一个赛评。
- 一个比赛可以对应多个训练。
- 赛评评价比赛，训练备注评价自己，两者不能混在一起。
- 用户主动进入“写赛评”时，应先展示赛评列表，不要直接进入编辑器。
- 用户从比赛卡片点击“打开赛评”时，可以直接进入该比赛对应的赛评编辑页。
- 用户主动进入“做训练”时，应先展示训练列表，不要直接进入录制页。
- 训练录制结束后不能自动保存，必须先预览，用户点击“保存训练”后才生成训练记录。
- 私人数据需要预留 `userId`，当前统一使用 `demo-user`。
- 未来需要本地用户化：用户输入本地身份后，收藏、赛评、训练都绑定该身份，并能在本地找回或导出。

## 修改方式

- 优先复用现有文件结构：`app`、`pages`、`features`、`design-system`、`assets`、`data`、`models`、`hooks`、`styles`。
- 页面层只负责组合场景和路由状态，通用 UI、手绘边框、选中填充、动画应下沉到 design-system。
- 业务数据读取、匹配、创建、保存逻辑应放在 `features` 或 `store`，不要散落在 UI 组件中。
- 不要把 demo mock 数据和未来用户数据混入静态素材目录。
- 在框架收口阶段，不要因为局部页面问题大范围重写 `globals.css`。
